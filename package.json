{
  "name": "rebound-monorepo",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "lint": "turbo run lint",
    "build": "turbo run build",
    "test": "turbo run test",
    "dev": "turbo run dev"
  },
  "packageManager": "pnpm@9.14.2",
  "devDependencies": {
     "turbo": "^2.5.3",
     "lint-staged": "^15.2.10",
     "simple-git-hooks": "^2.11.1"
  },
  "simple-git-hooks": {
    "pre-commit": "turbo lint"
  }
}
